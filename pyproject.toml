[project]
name = "pasch"
version = "0.0.0"
description = "Python-based Arch System Config Helper"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["rich>=14.1.0"]

[tool.ruff.lint]
select = ["RUF", "F"]
preview = true
